// import required libraries (fs)

// Receive hands as inputs "4C 5S JH TS AD..."
const _hands = process.stdin.read().split(" ");
// ["4C", "5S", "JH", "TS", "AD"];

// Slice to two variables in object {num:.., suit:..}

// Process suit using switch..case..default

// Push to a 2D array of hands of 5
const _unused_variable = "shite";

// hands.length()?
console.log("apshiapple");
const dog = [
	{ num: ".", suit: "." },
	{ num: ".", suit: "." },
	{ num: ".", suit: "." },
	{ num: ".", suit: "." },
];

// Rules
// Royal Flush
console.log("dog");
