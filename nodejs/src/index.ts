// import required libraries (fs)

// Receive hands as inputs via process.stdin..., e.g. 4C 5S JH TS AD
const hands = process.stdin.read().split(" ");

// Split to array at spaces ['4C','5S','JH','TS','AD']

// Empty array called emptyArray

// Slice to two variables in object {num:.., suit:..}

// Process suit using switch..case..default

// Push to a 2D array of hands of 5

// hands.length()?

